export enum SpecializationTag {
    INVERTER = 'inverter',
    SPLIT = 'split',
    WINDOW = 'window',
    CENTRAL = 'central',
    COMMERCIAL = 'commercial',
    BUDGET = 'budget',
    PREMIUM = 'premium',
    GENERALIST = 'generalist',
}
